// core 3 algor. relational: varaibles are values, and goal:s0->s1s pursues var/val relation
 // a=b=c transitive
 // a=b, b=a commutative &a=[1,_2nd] destruct
 // k=v var means val, deref all vars(relation solve units)

 // useEquiv, config

// pairedIdx,noSplitIf,subPaired required for interpreter opTree split-parse
// parse infix operator+precedence table
// query/load API
// just ask 1+2=x result
// mk.y E-DSL&funcDef API

// reversable +-*/
// possible st-combination enumerate chunk size
// st is copied only at eq(k,v)
// unify supports Type a/o. null is value(nil-LinkedList, etc.)
// support custom unify rule(like list(x,ra=restA) )
// occur check(off for perf?)
// lazy DFS
// lazy recursion goal .( *-*)-Zzz
